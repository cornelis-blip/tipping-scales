# bulk-associations — environment

# Required. Private app token with read/write on the object types you'll associate,
# e.g. crm.objects.contacts.write, crm.objects.companies.write, crm.objects.deals.write.
# (Association read/write is covered by the object scopes.)
HUBSPOT_TOKEN=pat-na1-...
